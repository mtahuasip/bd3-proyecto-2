NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api # URL base del backend accesible desde el cliente (Next.js necesita el prefijo NEXT_PUBLIC)

JWT_ACCESS_COOKIE_NAME=cookie_name # Nombre de la cookie que almacena el token JWT de acceso
